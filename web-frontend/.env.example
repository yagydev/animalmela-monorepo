# Copy to .env.local for local overrides.
# Vercel: do not rely on monorepo root .env — set MONGODB_URI and DATABASE_URL
# in the Vercel project (see env.production.example).
#
# Stack: Next.js + Tailwind + TS (this app) → Nest marketplace API → PostgreSQL/Prisma; auth JWT+OTP; product images via S3 presign from API.

# MongoDB — same Atlas cluster/db as monorepo (replace USER:PASSWORD). Use both keys; code reads either.
DATABASE_URL=mongodb+srv://USER:PASSWORD@cluster0.tpeb1bh.mongodb.net/kisaanmela_db_dev
MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster0.tpeb1bh.mongodb.net/kisaanmela_db_dev

# Main KisaanMela backend (Mongo / Express-style APIs)
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Nest + Prisma KisaanMela marketplace API (UI: /marketplace/kisaan)
# Production default is https://api.kisaanmela.com/api if unset. Optional server-only:
# MARKETPLACE_API_URL=http://localhost:4000/api
NEXT_PUBLIC_KISAANMELA_MARKETPLACE_API_URL=http://localhost:4000/api
# Server GETs retry https://api.kisaanmela.com/api when the primary host fails if: NODE_ENV=development,
# or the host is localhost / 127.0.0.1 / ::1 / host.docker.internal / RFC1918 LAN IP. Opt out:
# DISABLE_KISAAN_MARKETPLACE_PUBLIC_FALLBACK=1
# Optional alternate retry host: KISAANMELA_MARKETPLACE_FALLBACK_API_URL=https://api.kisaanmela.com/api
# Browser (cart/login) still uses NEXT_PUBLIC_* only — point it at production if you skip local Nest.
# Legacy alias (still read if set):
# NEXT_PUBLIC_AGRI_MARKETPLACE_API_URL=http://localhost:4000/api
